export function Footer() {
  return (
    <footer className="border-t border-border bg-white">
      <div className="mx-auto max-w-7xl px-4 py-8 text-sm text-text-secondary sm:px-6 lg:px-8">
        <p>SourceLink maps approved content to downstream usage and makes source drift visible before it becomes brand risk.</p>
      </div>
    </footer>
  );
}
